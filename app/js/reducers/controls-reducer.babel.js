const INITIAL_STATE = {
  isCode: false,
  isMinimize: false,
  isActive: false
};

const controls = (state = INITIAL_STATE, action) => {
  // switch (action.type) {
  // }
  return state;
};

export default controls;